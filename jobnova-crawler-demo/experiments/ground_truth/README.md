# Manually verified ground truth

Place one UTF-8 text file here for every page in `../pages.json`. Use the manifest id as the filename, preserve section headings and prefix each bullet with `- `.

Do not set a manifest entry to `verified: true` until every heading, paragraph, and bullet has been compared with the rendered live page and all unrelated page content has been removed.

